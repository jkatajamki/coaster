{
  "project": "Coaster",
  "port": "FROM_ENV",
  "db": {
    "host": "FROM_ENV",
    "port": "FROM_ENV",
    "database": "FROM_ENV",
    "user": "FROM_ENV",
    "password": "FROM_ENV",
    "max": 10,
    "idleTimeoutMillis": 30000,
    "env": "FROM_ENV"
  }
}
